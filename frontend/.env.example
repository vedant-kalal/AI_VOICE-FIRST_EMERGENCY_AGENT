# Where the FastAPI backend runs (the Vite dev server proxies /api and /ws to it)
VITE_BACKEND_URL=http://127.0.0.1:8000
# Only if the backend sets DASHBOARD_API_KEY — sent as X-API-Key (REST) and ?key= (WebSocket)
VITE_DASHBOARD_API_KEY=
